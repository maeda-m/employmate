<%= form_with(url: survey_answers_path(survey_id: @survey.id), method: :post, data: { controller: 'survey-profiles', action: 'turbo:submit-end->survey-profiles#restoreOriginFormAction' }) do |form| %>
  <%= render(Questionnaire::Component.with_collection(@questionnaires)) %>

  <%= turbo_frame_tag('survey-profiles') do %>
    <%= form.submit '結果を見る', hidden: true %>
  <% end %>
<% end %>
